= form_for course, :html => { :multipart => true } do |form|
  = render 'shared/error_messages', :instance => course 
  = form.label 'Nombre'
  = form.text_field :name
  = submit_tag 'Crear/Actualizar'